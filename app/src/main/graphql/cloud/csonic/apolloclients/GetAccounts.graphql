# Write your query or mutation here
query getAccountsByIdc ($identifier: DocumentInput!) {
  getCustomersByIdc(identifier: $identifier) {
    accounts{
      id
      number
      balance
      currency
    }
  }
}